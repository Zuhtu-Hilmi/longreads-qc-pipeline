# =============================================================================
# Pipeline Configuration
# =============================================================================
# Path to the input FASTQ file (long-read sequencing data)
input_fastq: "data/barcode77.fastq.gz"

# Output directory for all results
output_dir: "results"
